{
  "name": "vueupload",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build --base=/vueupload",
    "preview": "vite preview",
    "prebuild": "rm -rf dist",
    "postbuild": "rm -rf docs && mv dist docs && git add -f docs"
  },
  "dependencies": {
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.0",
    "vue-tsc": "^1.0.24"
  }
}
